App As appinfo:
    BackEnabled: =true
    ConfirmExit: =true
    OnStart: |
        =Set(varuser,User().FullName&Char(13))

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

